Useful project templates for `pdm`.

Contains templates:

- `minimal` - only `pyproject.toml` and `README.md`,
- `default` - for complete `python` projects,
- `notebook` - for working with `jupyter notebooks`,
- `plugin` - for `pdm` plugins development.
